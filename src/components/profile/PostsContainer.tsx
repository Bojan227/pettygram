export const PostsContainer = () => {
  return (
    <div className="posts-container">
      {/* {mockData.map((data) => {
        return (
          <img
            key={data.createdBy}
            src={data.images[0]}
            style={{ width: '300px', height: '300px' }}
          />
        );
      })} */}
    </div>
  );
};
